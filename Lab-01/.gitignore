__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.DS_Store

# Runtime mock state (seeded from mocks/seeds/ on first run; reset with `python reset.py`)
mocks/data/*.json

# Runtime episodic memory (one .md per customer; Bob's is committed as the seed)
memory/episodic/customer_*.md
!memory/episodic/customer_cust_002.md

# Runtime conversation memory (Strands FileSessionManager — per-customer chat history)
memory/sessions/

# REPL line-edit history
.repl_history

*.swp
