__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.DS_Store

# Runtime mock state (re-seeded from mocks/seeds/ on first run, or via
# the /api/reset endpoint on either service). Shared by v1 and v2 — both
# agents read and write the same files. See mocks/client.py.
mocks/data/*.json

# Web build artefacts and dependencies.
web/dist/
web/node_modules/

# v2 episodic memory (one .md per customer; Bob's seed file is committed).
cs_agent_v2/memory/episodic/customer_*.md
!cs_agent_v2/memory/episodic/customer_cust_002.md

# v2 conversation memory (Strands FileSessionManager — per-customer history).
cs_agent_v2/memory/sessions/

*.swp
