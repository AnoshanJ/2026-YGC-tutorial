// Copy this file to ./config.js and fill in the agent's runtime URL + API key.
// config.js is gitignored so the key never gets committed.
window.AGENT_CONFIG = {
  url: "https://your-am-gateway/agents/lab-03-cs-agent/default",
  apiKey: "sk_..."
};
