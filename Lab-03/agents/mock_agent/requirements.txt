# Intentionally empty: this placeholder uses stdlib only (http.server, json,
# os). The file exists so Google Buildpacks detects Python and the build
# runs the runtime layer without spending time on pip installs.
